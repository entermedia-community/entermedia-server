number_of_shards: 5
number_of_replicas: 1
#mapping._id.indexed: true
#mapper.dynamic: true
analysis: 
 analyzer: 
  lowersnowball:
   tokenizer: "standard"
   filter:
    - standard
    - lowercase
    - stemfilter
 analyzer:
  tags:
   type: "custom"
   tokenizer: "keyword"
   filter:
    - lowercase
 filter:
  stemfilter:
   type: "snowball"
   language: "English"