{
  "response":
      {
        "status":"ok",
        "id":"$data.id"
      }
     , 
   "data" :
  	$context.putPageValue("hit",$data)
	$pages.include(	"${apphome}/services/module/librarycollection/hit.json",$context)
}