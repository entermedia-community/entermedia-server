## This is really only used for annotations

[
#foreach($hit in $hits)
 
 #if($velocityCount > 1)
 ,
 #end
 {
 "id": "${hit.id}",
 "sourcepath": "${hit.sourcepath}",
 "annotated":false,
 "name": "${hit.name}"
 } 
#end
]

