## This is really only used for annotations

[
#foreach($hit in $hits)
 
 #if(${foreach.count} > 1)
 ,
 #end
 {
 "id": "${hit.id}",
 "sourcepath": "${hit.sourcepath}",
 "annotated":false,
 "assettype":"${hit.assettype}",
 "name": "${hit.name}"
 } 
#end
]

