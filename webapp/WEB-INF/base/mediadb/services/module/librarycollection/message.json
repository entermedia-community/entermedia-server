#jsonpartial($message $message.getSearcher().getPropertyDetails())

#set( $reactions = $reactionspermessage.get($message.getId()))
#ifnotnull( $reactions )
    , 
    "reactions": [
	 #foreach($reaction in $reactions)#if( ${foreach.count} != 1 ), #end
	   {
	   	  #jsonfields($reaction $mediaarchive.getSearcher("chatterboxreaction").getPropertyDetails()) 
	   }
	 #end
	 ]			  	
#end
#set( $attachments = $attachmentspermessage.get($message.getId()))
#ifnotnull( $attachments )
    , 
    "attachments": [
	 #foreach($attachment in $attachments)#if( ${foreach.count} != 1 ), #end
	   {
	   	  #jsonfields($attachment $mediaarchive.getSearcher("chatterboxattachment").getPropertyDetails()) 
	   }
	 #end
	 ]			  	
#end
