#set ($status = "error")
#set ($imagefile = $asset.imagefile)
#if ($imagefile)
  #set ($status = "ok")
#end
{
  "response":
  		{
			"status":"$status"
  		},
  "results":
  	{ 	
  		"asset":"$asset.id",
  		"filename":#jesc($asset.getPrimaryFile()),
  		"imagefilename":#jesc($imagefile)
  	}
}
