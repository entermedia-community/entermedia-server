WEBVTT

#set( $counter = 1 )
#foreach($track in $tracks) #set( $track = $captionsearcher.loadData($track) )
#foreach($caption in $track.getValue("captions"))
$counter
#set( $counter = $counter + 1 )
$track.formatTime($caption.timecodestart) --> $track.formatTime($caption.timecodeend)
$caption.captiontext

#end
#end
