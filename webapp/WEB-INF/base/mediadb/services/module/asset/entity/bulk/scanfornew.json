{
  "response":
		{
		#if( $newfolders)
				"status":"ok"
		#else 
				"status": "notfound" 
		#end
			#ifnotnull($newfolders)
			,"size":"$$newfolders.size()" 
			#end
		}
  ,
  "newfolders": [
	#foreach( $newfolder in $newfolders) #if( ${foreach.count} != 1 ), #end
	$newfolder
	#end
    ]
  , "existingfolders": [
	#foreach( $folder in $existingfolders) #if( ${foreach.count} != 1 ), #end
		{
			"id": "$folder.getId()",
			"name": "$folder.getName()",
			"path": "${folder.entitysourcetype}"
		}
	#end
    ]
}