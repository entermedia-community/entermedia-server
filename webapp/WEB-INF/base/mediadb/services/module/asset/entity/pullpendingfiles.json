#set( $files = $assetmap.files )
  {
  "files" : [
  #foreach( $file in $files) #if( ${foreach.count} != 1 ), #end
  	{ 
		"path": "$file.path",
  		"url": "$file.url",
  		"size":"$file.size",
  		"id": "$file.id"
  		}
	#end
 	]
	
}