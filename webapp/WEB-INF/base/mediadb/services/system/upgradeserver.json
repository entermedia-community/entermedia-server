#set ($status = "ok")
#if( $error )
	#set ($status = $error)	
#end
{
"response":
	{
	    "status":"$status"
	},
"results":[
#foreach( $id in $upgrader.list )
	#if( $velocityCount != 1 ), #end
	"$id"
	]
#end 
]
}