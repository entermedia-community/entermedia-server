{
#set($newpreset = $context.findValue("newpreset"))
#set($type =$mediaarchive.getMediaRenderType($asset.fileformat))
#set($hits = $conversionUtil.getActivePresetList($catalogid,$type) )
	"response":
		{
			"totalhits" : "$hits.size()",
			"searchtype" : "presetdata",
			"page": 1,
			"pages": 1,
			"query":
			{
				"search":"assetid:$asset.id"
			}
		},
	"results":[
#set( $cdn = $mediaarchive.getCatalogSettingValue("cdn_prefix"))	
#foreach( $presetdata in $hits )#if( $velocityCount != 1 ), #end
{	
	"name":#jesc($presetdata.name),
	#set( $outputfile = "${presetdata.outputfile}/$mediaarchive.asExportFileName($asset, $presetdata)")
	"outputfile":#jesc($outputfile),
	#set( $url = "$!cdn$home$apphome/services/modules/assets/downloads/generatedpreview/${asset.sourcepath}/$outputfile")
	"url":"$url",
	#set($isready = $conversionUtil.isConvertPresetReady($catalogid,$asset.id,$asset.sourcePath, $presetdata.id))
	"iscomplete":"$isready"}#end
]
}
