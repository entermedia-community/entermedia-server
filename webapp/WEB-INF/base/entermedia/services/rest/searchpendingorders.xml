<?xml version="1.0" encoding="utf-8" ?>
#if( $orders && $orders.size() > 0 )
<rsp stat="ok">
#foreach( $hit in $orders )
	<order id="$hit.id" >
#foreach( $item in $orderManager.findOrderItems($context,$catalogid,$hit.id) )
		<orderitem id="$item.id" assetsourcepath="$item.assetsourcepath" presetid="$orderManager.getPresetForOrderItem($item)" destinationid="$orderManager.getPublishDestinationForOrderItem($item)" status="$hit.status" />
#end
	</order>
#end
#else
<rsp stat="fail">
Login failed or no data found
#end
</rsp>