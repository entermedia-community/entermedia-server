
#set( $fields = $store.getProductSearcher().getDataProperties("reportsearch",$user) )
#foreach( $field in $fields )$field.id	#end

#foreach( $hit in $store.getProductSearcher().getAllHits() )   ##hits
#set( $product = false )
#set( $product = $store.getProduct($hit.id) )
#set($blank = $store.getProductArchive().clearProduct($product))
#if( $product.isAvailable() )
#foreach( $field in $fields )#if($field.isList())#set( $types = $store.getProperties(${field.getId()}))$!types.get($product.get($field.getId() ))#else$!product.get($field.id)#end	#end

#end
#end
